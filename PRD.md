# 学习伴侣 App — 产品需求文档 (PRD)

> 版本：v1.0 | 最后更新：2026-07-27 | 状态：已上线

---

## 一、产品概述

### 1.1 产品定位
个人学习工具，面向数据产品经理，覆盖 SQL、Python、数据分析、DAMA 认证等科目的学习、练习和知识扩展。通过 AI 辅助降低理解成本，通过多端同步实现随时随地的碎片化学习。

### 1.2 目标用户
- 数据产品经理（核心）
- 需要学习 SQL/Python/数据分析/DAMA 的技术从业者
- 个人学习场景，非 SaaS/多租户

### 1.3 核心理念
- **白话入门**：用生活比喻降低理解门槛
- **学练结合**：边学边练，收藏难点反复看
- **AI 辅助**：随时提问扩展知识
- **多端同步**：手机、平板、Mac 学习进度一致

---

## 二、功能全景

| 模块 | 页面 | 核心功能 |
|------|------|---------|
| 首页 | `/` | 科目概览、继续学习、大白话、成就统计 |
| 学习 | `/learn/:id` | 课程内容、代码块复制、笔记 |
| 练习 | `/quiz` | 选择题刷题、收藏、对错反馈 |
| 题库 | `/bank` | 全部题目浏览、按科目/收藏筛选、答案查看 |
| 编译器 | `/compiler` | SQL/Python 语法校验、示例代码 |
| AI 助手 | 全局浮窗 | 智能问答、知识扩展、拖拽吸附 |
| 同步 | 全局 | 多设备进度同步 |

---

## 三、功能详情

### 3.1 首页 Dashboard

**内容区域：**
- **顶部状态栏** — 学习伴侣 Logo、XP 积分、连续学习天数、同步按钮
- **继续学习卡片** — 高亮展示上次学习进度的课程，带进度条和播放按钮
- **科目 2×2 网格** — SQL / Python / 数据分析 / DAMA，每个带环形进度图
- **大白话卡片** — 随机展示一条技术概念的日常比喻，点击切换
- **底部成就条** — 已掌握/待复习/收藏题目/今日学习 四项统计

**交互规则：**
- 点击科目进入科目详情页
- 同步按钮点击后上传本地进度 + 拉取远端进度，显示 loading 和 done 状态

### 3.2 科目详情

**内容区域：**
- 返回按钮、科目名称、完成百分比
- 课程阶梯列表，每项展示：圆形序号（已完成/学习中/未开始）、标题、小节数、时长、状态标签

**交互规则：**
- 已完成/学习中的课程可点击进入学习页
- 未开始的课程灰色不可点击

### 3.3 学习内容页

**内容区域：**
- 顶部导航、章节进度
- 三个 Tab：学习 / 笔记 / 练习
- 内容块：标题 + 正文 + 代码块（带复制按钮）
- 底部导航：上一节 / 标记完成

**交互规则：**
- 代码块右上角「复制」按钮，点击复制并显示「✓ 已复制」
- 「标记完成」更新本地 IndexedDB 并通过 sync 推送到 API

### 3.4 练习页

**内容区域：**
- 筛选栏：全部 / SQL / Python / 数据分析 / DAMA / ⭐ 收藏
- 题目卡片：题号、题目文本、4 个选项（A/B/C/D）
- 每道题右上角收藏按钮（☆/⭐）
- 提交后显示结果卡片：🎉 正确 / 😅 错误 + 正确答案
- 下一题按钮随机切换题目

**交互规则：**
- 选择后高亮蓝色边框，提交前可修改
- 提交后锁定选项，正确/错误标记不同颜色
- 收藏状态与题库页共享，跨页面同步

### 3.5 题库页

**内容区域：**
- 筛选栏：全部 / SQL / Python / 数据分析 / DAMA / ⭐ 收藏（跟练习页一致）
- 题目列表卡片：题目文本、科目标签、收藏按钮、查看答案/收起答案

**交互规则：**
- 查看答案展开灰色答案区域，收起折叠
- 收藏状态与练习页共享
- 筛选结果实时更新，显示「共 N 题」

### 3.6 代码编译器

**内容区域：**
- 语言切换 Tab：SQL / Python
- 深色代码编辑器（textarea）
- 校验结果输出区（绿色通过/红色报错）
- 校验按钮 + 示例按钮 + Shift+Enter 快捷键提示

**校验逻辑：**
- SQL：检查是否以 SELECT/INSERT/UPDATE/DELETE/CREATE 开头、括号匹配
- Python：用 `new Function()` 做语法校验

### 3.7 AI 助手（核心特色功能）

**触发方式：**
- 全局浮动圆形按钮，蓝紫渐变（`#89b4fa → #b4befe`）
- 选中任意文字后出现浮动「🤖 问助手」按钮

**弹窗交互：**
- 深色主题弹窗（Catppuccin Mocha 色系）
- 顶部：配置表单（API 端点 / API Key / 模型名）、保存按钮
- 中部：聊天消息区域，用户消息蓝色、AI 消息深色
- 底部：输入框 + 发送按钮，支持 Enter 发送

**AI 能力：**
- 当前对接：DeepSeek API（`deepseek-chat`）
- 回复内容支持：**粗体**、行内代码、代码块、多行排版
- 也可接入火山引擎 Doubao、OpenAI 等兼容接口

**拖拽与吸附：**
- 按钮可自由拖拽到屏幕任意位置
- 拖到左右侧边 50px 以内时吸附成细黑竖杠（6px）
- 吸附后热区扩大（padding 14px × 2），视觉不变
- 按住竖杠向屏幕中心拖拽超过 40px 松手 → 弹窗自动弹出
- Y 轴限制在 40px ~ 视口高度-120px 之间，不会拖丢

### 3.8 选中文字快速提问

- 选中任意页面上超过 2 个字符的文字
- 文字上方出现浮动按钮「🤖 问助手」
- 点击按钮后 AI 助手弹窗打开，选中文字自动填入输入框

### 3.9 数据同步

**架构：**
- 前端 Dexie.js（IndexedDB）+ 后端 JSON 文件 API
- API 服务运行在用户 Mac 上（端口 8086）
- 通过 Tailscale 加密通道跨设备访问

**同步内容：**
- 学习进度（章节完成状态）
- 收藏的题目
- 学习笔记

**同步策略：**
- 每 30 秒自动增量同步
- 首页提供手动同步按钮（🔄 同步 → ⟳ 同步中 → ✓ 已同步）
- 同步不上时降级为本地存储，不影响使用

---

## 四、技术方案

### 4.1 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 前端框架 | React 19 + TypeScript | |
| 构建工具 | Vite 8 | |
| 路由 | react-router-dom v7 | |
| 本地存储 | Dexie.js (IndexedDB) | 离线可用 |
| 组件 | 无 UI 库，手写 inline styles | |
| 图标 | 内联 SVG | |
| 后端服务 | Node.js HTTP Server | 端口 8086 |
| 数据同步 | JSON 文件存储 + REST API | |
| 网络 | Tailscale | 跨设备加密通信 |
| PWA | vite-plugin-pwa | manifest + Service Worker |

### 4.2 部署架构

```
Mac (服务器端)
├── 前端服务 :8085 — 学习伴侣 App（静态文件 + SPA 路由）
├── API 服务 :8086 — 数据同步（读写 data.json）
└── data.json — 所有同步数据

iPhone/iPad (客户端)
└── Safari → http://100.101.115.91:8085/
              (Tailscale 私有网络)
```

### 4.3 项目结构

```
stj/
├── src/
│   ├── components/
│   │   ├── BottomNav.tsx        # 底部导航
│   │   ├── AIAssistant.tsx      # AI 助手（弹窗 + 拖拽按钮）
│   │   └── SelectionTooltip.tsx # 选中文字快速提问
│   ├── pages/
│   │   ├── Home.tsx             # 首页
│   │   ├── SubjectDetail.tsx    # 科目详情
│   │   ├── Learn.tsx            # 学习内容
│   │   ├── Quiz.tsx             # 练习
│   │   ├── Bank.tsx             # 题库
│   │   ├── Compiler.tsx         # 编译器
│   │   └── Notes.tsx            # 笔记
│   ├── data/
│   │   ├── tips.ts              # 大白话数据
│   │   ├── questions.ts         # 题库数据
│   │   ├── chapters.ts          # 课程数据
│   │   └── content.ts           # 学习内容数据
│   ├── store/
│   │   └── db.ts                # IndexedDB + 同步逻辑
│   ├── api.ts                   # 后端 API 客户端
│   ├── App.tsx                  # 路由 + 布局
│   ├── index.css / App.css      # 全局样式
│   └── main.tsx                 # 入口 + SW 清理
├── server-api.mjs               # 后端同步 API
├── public/icons/                # PWA 图标
└── PRD.md                       # 本文档
```

---

## 五、设计规范

### 5.1 色彩体系

| 用途 | 颜色 | 备注 |
|------|------|------|
| 弹窗背景 | `#1e1e2e` | Catppuccin Mocha base |
| 聊天区背景 | `#11111b` | crust |
| AI 消息背景 | `#313244` | surface0 |
| 用户消息背景 | `#89b4fa` | 蓝色 |
| 主要文字 | `#cdd6f4` | text |
| 次要文字 | `#a6adc8` | subtext0 |
| 弱化文字 | `#585b70` | overlay0 |
| 分割线 | `#313244` | surface0 |
| AI 按钮渐变 | `#89b4fa → #b4befe` | 蓝紫 |
| 吸附竖杠 | `#1e1e2e` | 与弹窗同色 |

### 5.2 交互规范

| 交互 | 规范 |
|------|------|
| 圆角(弹窗) | 24px + overflow:hidden |
| 圆角(卡片/按钮) | 8-14px |
| 圆角(头像) | 50% |
| 动画缓动 | `cubic-bezier(.22,1,.36,1)` |
| 动画时长 | 250-400ms |
| 拖拽吸附距离 | 距侧边 < 50px |
| 拖拽弹出距离 | 从吸附位拖 > 40px |
| 按钮热区 | 视觉 6px + padding 14px×2 = 34px 实际热区 |
| Y 轴约束 | 40px ~ (视口高度-120px) |

### 5.3 暗色主题规则

- 弹窗、输入框、按钮背景统一用 `#1e1e2e`
- 聊天区域用更深的 `#11111b` 做视觉区分
- 文字三层：`#cdd6f4` / `#a6adc8` / `#585b70`
- 分割线用 `#313244`，不突兀
- 用户消息用蓝色 `#89b4fa`（深色背景上更明显）

---

## 六、数据模型

### 6.1 本地 IndexedDB

```typescript
questions: { id, subj, q, answer, star }
notes:     { id, courseId, title, content, createdAt, _id }
progress:  { chapterId, completed, updatedAt }
```

### 6.2 后端 JSON 存储

```json
{
  "devices": { "uuid": { "name", "lastSeen" } },
  "progress": { "chapterId": { "deviceId": true } },
  "stars": { "questionId": { "deviceId": true/false } },
  "notes": [ { "_id", "courseId", "title", "content", "createdAt", "_device" } ]
}
```

### 6.3 同步合并策略

- progress：多设备之间，任意设备最新写入覆盖
- stars：多设备之间，任意设备最新写入覆盖
- notes：去重（基于 `_id`），不覆盖已有笔记

---

## 七、局限性 & 下一步

### 7.1 当前局限
- 无用户登录系统，基于 localStorage + 设备 ID 做区分
- 无云端数据库，数据存储在 Mac 本地 JSON 文件
- 依赖 Tailscale 进行跨设备通信
- Mac 关机后服务不可用

### 7.2 可选迭代方向
- 支持 Supabase 作为云端数据库
- 增加用户注册/登录
- AI 自动生成课程内容（输入主题生成学习材料）
- 丰富的学习统计（学习曲线、弱项分析）
- 支持离线笔记编辑 + 延迟同步
